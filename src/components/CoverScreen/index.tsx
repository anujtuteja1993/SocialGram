const CoverScreen = () => {
    return (
        <div className="fixed top-0 left-0 w-full h-full bg-black/50 flex justify-center items-center z-[1000] backdrop-blur" />
    );
};

export default CoverScreen;
